{
  "name": "@dialogue-constructor/frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-5173}",
    "preview": "vite preview"
  }
}
