# 📱 Веб-интерфейс для редактирования схем ботов

Минимальный веб-интерфейс для редактирования JSON-схем ботов через браузер.

## 🚀 Быстрый старт

### Локальная разработка

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Настройте API URL:**
   Откройте `script.js` и измените `API_BASE_URL` на адрес вашего API сервера:
   ```javascript
   const API_BASE_URL = 'http://localhost:3000';
   ```

3. **Настройте Telegram Login Widget:**
   Откройте `index.html` и замените `YOUR_BOT_USERNAME` на username вашего бота:
   ```html
   <script async src="https://telegram.org/js/telegram-widget.js?22" 
           data-telegram-login="YOUR_BOT_USERNAME" ...>
   ```

4. **Запустите локальный сервер:**
   ```bash
   # Используя Python
   python3 -m http.server 8000
   
   # Или используя Node.js
   npx http-server -p 8000
   
   # Или используя PHP
   php -S localhost:8000
   ```

5. **Откройте в браузере:**
   ```
   http://localhost:8000
   ```

## 📦 Развертывание на Vercel

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в Vercel:**
   ```bash
   vercel login
   ```

3. **Разверните проект:**
   ```bash
   cd packages/frontend
   vercel
   ```

4. **Настройте переменные окружения:**
   В настройках проекта на Vercel добавьте:
   - `API_BASE_URL` - URL вашего API сервера

5. **Обновите `script.js`:**
   Измените `API_BASE_URL` на URL вашего API:
   ```javascript
   const API_BASE_URL = 'https://your-api-domain.com';
   ```

## 🔧 Настройка API

Убедитесь, что в основном боте (`packages/core`) настроен CORS:

1. **Установите переменную окружения:**
   ```bash
   FRONTEND_URL=https://your-frontend.vercel.app
   ```

2. **API endpoints должны быть доступны:**
   - `GET /api/bots?user_id={user_id}` - список ботов
   - `GET /api/bot/:id/schema` - получить схему
   - `POST /api/bot/:id/schema` - обновить схему

## 🔐 Авторизация

Интерфейс использует Telegram Login Widget для авторизации. 

**Важно:** Для работы в продакшене необходимо:
1. Настроить домен в настройках бота через @BotFather
2. Указать правильный `data-telegram-login` в `index.html`

## 📝 Использование

1. **Войдите через Telegram:**
   - Нажмите кнопку "Login with Telegram"
   - Авторизуйтесь через Telegram

2. **Выберите бота:**
   - Выберите бота из списка
   - Нажмите "Загрузить схему"

3. **Редактируйте схему:**
   - Отредактируйте JSON в текстовом редакторе
   - Используйте "Проверить JSON" для валидации
   - Используйте "Форматировать" для красивого форматирования

4. **Сохраните:**
   - Нажмите "Сохранить схему"
   - Проверьте предпросмотр обновленной схемы

## 🎨 Особенности

- ✅ Валидация JSON с подсветкой ошибок
- ✅ Автоматическое форматирование
- ✅ Предпросмотр схемы
- ✅ Авторизация через Telegram
- ✅ Адаптивный дизайн
- ✅ Чистый HTML/CSS/JS без фреймворков

## 🐛 Устранение проблем

### Ошибка CORS
Убедитесь, что `FRONTEND_URL` в `.env` основного бота совпадает с URL вашего фронтенда.

### Ошибка авторизации
Проверьте, что:
- Username бота указан правильно в `index.html`
- Домен добавлен в настройках бота через @BotFather

### API не отвечает
Проверьте:
- API сервер запущен
- `API_BASE_URL` указан правильно
- CORS настроен корректно

