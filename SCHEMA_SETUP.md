# 📋 Инструкция по установке схемы через /editschema

## 📝 Формат команды

```
/editschema <bot_id> <json_schema>
```

## 🔍 Шаг 1: Получите ID вашего бота

1. Используйте команду `/my_bots` в основном боте
2. Скопируйте ID бота (UUID формат)

## 📦 Шаг 2: Подготовьте JSON-схему

### Структура схемы:

```json
{
  "version": 1,
  "states": {
    "state_name": {
      "message": "Текст сообщения",
      "buttons": [
        {
          "text": "Текст кнопки",
          "nextState": "next_state_name"
        }
      ]
    }
  },
  "initialState": "state_name"
}
```

### Поля схемы:

- **`version`**: всегда должен быть `1`
- **`states`**: объект с состояниями диалога
  - Ключ: название состояния (строка)
  - Значение: объект состояния
    - `message` (обязательно): текст сообщения
    - `buttons` (опционально): массив кнопок
      - `text`: текст кнопки
      - `nextState`: название следующего состояния (должно существовать в `states`)
- **`initialState`**: название начального состояния (должно существовать в `states`)

## 📝 Примеры схем

### Пример 1: Простой диалог с двумя состояниями

```json
{
  "version": 1,
  "states": {
    "start": {
      "message": "Привет! Выберите действие:",
      "buttons": [
        { "text": "О нас", "nextState": "about" },
        { "text": "Контакты", "nextState": "contacts" }
      ]
    },
    "about": {
      "message": "Это бот-конструктор диалогов для Telegram."
    },
    "contacts": {
      "message": "Контакты: @username"
    }
  },
  "initialState": "start"
}
```

### Пример 2: Многоуровневое меню

```json
{
  "version": 1,
  "states": {
    "start": {
      "message": "Добро пожаловать! Выберите категорию:",
      "buttons": [
        { "text": "Услуги", "nextState": "services" },
        { "text": "О компании", "nextState": "company" },
        { "text": "Помощь", "nextState": "help" }
      ]
    },
    "services": {
      "message": "Наши услуги:\n\n• Разработка ботов\n• Консультации\n• Техподдержка",
      "buttons": [
        { "text": "⬅️ Назад", "nextState": "start" }
      ]
    },
    "company": {
      "message": "О нашей компании:\n\nМы создаем Telegram-ботов с 2020 года.",
      "buttons": [
        { "text": "⬅️ Назад", "nextState": "start" }
      ]
    },
    "help": {
      "message": "Нужна помощь? Напишите нам: @support",
      "buttons": [
        { "text": "⬅️ Назад", "nextState": "start" }
      ]
    }
  },
  "initialState": "start"
}
```

### Пример 3: Линейный диалог (последовательные шаги)

```json
{
  "version": 1,
  "states": {
    "step1": {
      "message": "Шаг 1: Добро пожаловать!",
      "buttons": [
        { "text": "Далее ➡️", "nextState": "step2" }
      ]
    },
    "step2": {
      "message": "Шаг 2: Инструкция по использованию",
      "buttons": [
        { "text": "⬅️ Назад", "nextState": "step1" },
        { "text": "Далее ➡️", "nextState": "step3" }
      ]
    },
    "step3": {
      "message": "Шаг 3: Завершение. Спасибо за внимание!",
      "buttons": [
        { "text": "⬅️ Назад", "nextState": "step2" },
        { "text": "🔄 Начать сначала", "nextState": "step1" }
      ]
    }
  },
  "initialState": "step1"
}
```

## 🚀 Шаг 3: Установите схему

### Вариант 1: В одну команду (для коротких схем)

Отправьте команду в Telegram:

```
/editschema YOUR_BOT_ID {"version":1,"states":{"start":{"message":"Привет!","buttons":[{"text":"Далее","nextState":"next"}]},"next":{"message":"Второй шаг"}},"initialState":"start"}
```

⚠️ **Внимание**: JSON должен быть в одну строку без переносов!

### Вариант 2: Минимизация JSON (для длинных схем)

Используйте онлайн-инструменты для минификации JSON:
- https://jsonformatter.org/json-minify
- https://codebeautify.org/jsonminifier

1. Подготовьте схему в читаемом формате
2. Минимизируйте JSON (уберите пробелы и переносы)
3. Скопируйте минифицированный JSON
4. Отправьте команду `/editschema <bot_id> <минифицированный_json>`

### Пример минифицированного JSON:

Было:
```json
{
  "version": 1,
  "states": {
    "start": {
      "message": "Привет!",
      "buttons": [
        { "text": "Далее", "nextState": "next" }
      ]
    }
  },
  "initialState": "start"
}
```

Стало:
```json
{"version":1,"states":{"start":{"message":"Привет!","buttons":[{"text":"Далее","nextState":"next"}]}},"initialState":"start"}
```

## ✅ Проверка результата

После успешной установки схемы вы получите сообщение:

```
✅ Схема успешно обновлена!

🤖 Бот: Название бота
🆔 ID: uuid-бота
📊 Состояний: 3
🔄 Версия схемы: 1

Начальное состояние: start
```

## 🔧 Устранение проблем

### Ошибка: "Неверный формат JSON"

- Убедитесь, что JSON валидный
- Проверьте правильность кавычек (должны быть двойные `"`)
- Убедитесь, что JSON в одну строку без переносов
- Используйте JSON валидатор: https://jsonlint.com/

### Ошибка: "Схема невалидна"

Проверьте:
- ✅ `version` равен `1`
- ✅ Все состояния имеют поле `message`
- ✅ `initialState` существует в `states`
- ✅ Все `nextState` в кнопках существуют в `states`
- ✅ `buttons` - это массив (не объект)

### Ошибка: "Бот с таким ID не найден"

- Убедитесь, что ID бота правильный
- Проверьте, что бот принадлежит вам (используйте `/my_bots`)
- ID должен быть в формате UUID

### Ошибка: "Сообщение слишком длинное"

Если JSON схема слишком большая:
1. Разбейте диалог на более мелкие состояния
2. Используйте более короткие тексты сообщений
3. Уменьшите количество кнопок в одном состоянии

## 💡 Советы

1. **Тестируйте схему локально** перед установкой:
   - Используйте JSON валидатор
   - Проверьте логику переходов между состояниями

2. **Используйте понятные названия состояний**:
   - Хорошо: `start`, `about`, `contacts`
   - Плохо: `s1`, `a1`, `c1`

3. **Добавьте кнопку "Назад"** для удобной навигации

4. **Ограничения Telegram**:
   - Максимальная длина сообщения: 4096 символов
   - Максимум кнопок в ряду: зависит от размера текста
   - Максимум кнопок в сообщении: 100

5. **Для очень сложных схем** рассмотрите возможность разбиения на несколько простых диалогов

## 📚 Дополнительная информация

После установки схемы:
1. Убедитесь, что webhook установлен для вашего бота (`/setwebhook <bot_id>`)
2. Протестируйте диалог, отправив сообщение вашему боту
3. Состояния пользователей хранятся в Redis (TTL: 30 дней)

