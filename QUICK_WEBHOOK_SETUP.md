# Быстрая настройка Webhook

## Текущая ситуация

Webhook не установлен, накопилось 5 обновлений. Бот не может отвечать.

## Решение: Установить webhook прямо сейчас

### Шаг 1: Узнайте ваш Vercel URL

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Выберите проект `packages/core` (или как он называется у вас)
3. Скопируйте URL проекта (например: `lego-bot-core.vercel.app` или `lego-bot-core-xxx.vercel.app`)

### Шаг 2: Установите webhook

Откройте в браузере (замените `YOUR_BOT_TOKEN` на токен из `.env` и `YOUR_VERCEL_URL` на ваш Vercel URL):

```
https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://YOUR_VERCEL_URL/api/webhook
```

**Пример:**
```
https://api.telegram.org/bot8585269589:AAGNheAjAdj5p6FJ6Xi-NCZk-fW3g1wYFDQ/setWebhook?url=https://lego-bot-core.vercel.app/api/webhook
```

### Шаг 3: Проверьте результат

Должен вернуться:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

Если ошибка - проверьте:
- Правильность токена
- Правильность URL (должен быть доступен из интернета)
- Что endpoint `/api/webhook` существует

### Шаг 4: Проверьте установку

```
https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
```

Должно быть:
```json
{
  "ok": true,
  "result": {
    "url": "https://YOUR_VERCEL_URL/api/webhook",
    "pending_update_count": 0
  }
}
```

### Шаг 5: Отправьте /start боту

После установки webhook:
1. Отправьте `/start` боту
2. Бот должен ответить (обработаются все накопленные обновления)
3. Проверьте логи Vercel - должны быть сообщения о получении обновлений

## Если не работает

1. **Проверьте логи Vercel:**
   - Должны быть сообщения `📨 Webhook received:` при отправке команды
   - Если нет - webhook не доходит до Vercel

2. **Проверьте URL:**
   - Откройте в браузере: `https://YOUR_VERCEL_URL/health`
   - Должен вернуться JSON с информацией о здоровье сервиса

3. **Проверьте переменные окружения в Vercel:**
   - `TELEGRAM_BOT_TOKEN` должен быть установлен
   - `VERCEL_URL` устанавливается автоматически

4. **Проверьте, что деплой завершился:**
   - В Vercel Dashboard должен быть успешный деплой
   - Проверьте, что нет ошибок сборки

